# H200 branch scope

The active experiment contract belongs to `dev-h200-new`. Other branches are
not certified to share its kernels, configuration pools, or model adapters.
See [the current contract](BENCHMARK_CONTRACT.md).

KDA uses `examples/kda/chunk_intra_token_parallel.py`. FP8 uses the original
`examples/gemm_fp8/example_tilelang_gemm_fp8.py` kernel and scheduling knobs.
