# H200 grouped GEMM heuristics

No exhaustive grouped GEMM sweep has been recorded here. The shared
`experiments.common.brute_force` runner writes audited winner JSONs after a full
pool sweep and repeated winner checks. See the [family commands](../../README.md).
Subset validation does not establish a tuned winner.
